Run the test suite and report results.

1. Run `pnpm test` from the repo root
2. If tests fail:
   - Show which tests failed and why
   - Identify the root cause (logic error, missing implementation, broken assertion)
   - Ask the user whether to fix the failures or just report them
3. If tests pass, confirm all green with a one-line summary (e.g. "42 passed, 0 failed")

Do not modify any test files unless explicitly asked.
